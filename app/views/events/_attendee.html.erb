<li>
  <div class="d-flex align-items-center gap-2 mb-3">
    <%= image_tag attendee.decorate.avatar, class: 'rounded-circle avatar-md' %>
    <%= link_to attendee.name, event_attendance_path(event_id: @event.id, id: attendee.id) %>
  </div>
</li>
